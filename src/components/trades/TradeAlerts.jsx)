// 1. Adicione o import
import TradeAlerts from './components/trades/TradeAlerts';

// 2. No componente App, defina o estado de configuração (baseado na entidade TradeConfig.json) [cite: 15]
const [tradeConfig] = useState({
  saldo_inicial: 10000,
  limite_perda_diaria: 500,
  limite_drawdown: 10
});

// 3. Na seção {activeTab === 'trades' && (...)}:
{activeTab === 'trades' && (
  <div className="space-y-8 animate-in slide-in-from-bottom-4 duration-700">
    <div className="grid grid-cols-1 lg:grid-cols-4 gap-6">
      <div className="lg:col-span-3">
        <TradeForecasting />
      </div>
      <div className="space-y-4">
        {/* Sistema de Alertas em tempo real [cite: 34] */}
        <TradeAlerts trades={sampleTrades} config={tradeConfig} />
        <TradeImporter onImportSuccess={() => toast.success("Dados processados!")} />
      </div>
    </div>
    
    <div className="bg-white dark:bg-slate-900 p-6 rounded-3xl border border-slate-200 dark:border-slate-800 shadow-sm">
      <h3 className="text-lg font-bold mb-6 flex items-center gap-2">
        <BarChart3 className="text-blue-600" size={20} />
        Performance Avançada [cite: 24]
      </h3>
      <AdvancedCharts trades={sampleTrades} initialBalance={tradeConfig.saldo_inicial} />
    </div>
  </div>
)}
